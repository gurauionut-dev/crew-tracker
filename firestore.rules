rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all (secured by app-level passwords)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
